You are a QA automation engineer.

Given the HTML of a webpage and a list of UI elements,
extract the most reliable Playwright selectors.

Rules:
- Never use brittle XPATH unless no alternative exists.
- Prefer: data-testid, aria-label, name, id, role, text()
- Return ONLY valid JSON. Do NOT include any extra text.
- Always use double quotes for keys and strings
- No trailing commas
- Root object must be enclosed in {}

Required output structure:

{
  "element_name" : "selector_string"
}

HTML:
